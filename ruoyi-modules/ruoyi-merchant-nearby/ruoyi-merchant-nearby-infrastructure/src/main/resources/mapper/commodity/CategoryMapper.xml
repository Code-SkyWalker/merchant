<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.merchant.infrastructure.commodity.mapper.CategoryMapper">
    <resultMap id="BaseResultMap" type="org.dromara.merchant.infrastructure.commodity.mapper.dataobject.CategoryDO">
        <!--@mbg.generated-->
        <!--@Table tb_category-->
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="goods_num" property="goodsNum"/>
        <result column="is_show" property="isShow"/>
        <result column="is_menu" property="isMenu"/>
        <result column="seq" property="seq"/>
        <result column="parent_id" property="parentId"/>
        <result column="merchant_id" property="merchantId"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="create_dept" property="createDept"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--@mbg.generated-->
        id,
        `name`,
        goods_num,
        is_show,
        is_menu,
        seq,
        parent_id,
        merchant_id,
        tenant_id,
        create_dept,
        create_by,
        create_time,
        update_by,
        update_time
    </sql>

    <select id="selectByParentId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from tb_category
        where parent_id = #{parentId}
    </select>

    <select id="selectAll" resultType="org.dromara.merchant.client.commodity.dto.data.clientobject.CategoryTreeCO">
        select
        <include refid="Base_Column_List"/>
        from tb_category
        where merchant_id = #{merchantId}
    </select>

    <select id="selectCascade"
            resultType="org.dromara.merchant.client.commodity.dto.data.clientobject.CategoryCascadeCO">
        select
        category.id,
        category.`name`,
        template.id AS template_id
        from tb_category category
        left join tb_template template on category.id = template.category_id
        where merchant_id = #{merchantId}
            and parent_id = #{parentId}
        order by seq desc
    </select>
</mapper>
